services:
  llm-gateway:
    image: golang:1.26-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "${GATEWAY_PORT:-1234}:1234"
    command: >
      sh -c "
        go build -o llm-gateway . &&
        ./llm-gateway
      "
